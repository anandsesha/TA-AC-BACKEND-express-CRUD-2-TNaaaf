var express = require('express');
var router = express.Router();
var Author = require('../models/author');
var Book = require('../models/book');

/* ---------- List (GET) Books -> using book Name ------------*/
// router.get('/:bookid/books');

module.exports = router;
